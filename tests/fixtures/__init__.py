# tests/fixtures — In-repo data fixtures for adversarial gate testing.
# These files are committed to version control because they are small,
# deliberately constructed test artefacts, NOT raw production data.
